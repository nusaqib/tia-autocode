# Intentionally broken spec - fixture for Test-TiaSpec (must FAIL validation).
project:
  name: Broken
plcs:
  - name: PLC_1
    modules:
      - data/modules.csv
    tags:
      - data/tags.csv
    udts:
      - data/udts.csv
    dbs:
      blocks: data/dbs.blocks.csv
    logic: []
hmis:
  - name: HMI_1
    tags:
      - data/hmitags.csv          # missing DataType + duplicate name (must error)
    screens:
      - hmi/DoesNotExist.xml       # missing file (must error)
